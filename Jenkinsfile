pipeline {
    agent any

    stages {
        stage('Clone-code') {
            steps {
                git branch: 'main', url: 'https://github.com/mominarmughan/DevOps-project.git'
            }
        }
    }
}
